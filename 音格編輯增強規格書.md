# 音格編輯增強規格書

- 文件狀態：待產品確認
- 規格日期：2026-08-31
- 依據版本：目前工作區版本（Git `0ef8d6f`）
- 影響範圍：輸譜頁、樂譜 JSON、播放／練唱流程
- 本期主題：3 拍音符、四分三連音、範圍複製／貼上、指定位置插入、練唱五線譜與播放跟隨高亮
- BPM 範圍：40–150；所有輸入、滑桿、匯入與播放統一套用此上限。
- 參考實作：`D:\beatnew\beatmaker-samba-V2`（VexFlow 轉譜、來源 step 映射、播放高亮快取與自動捲動）

## Executive Summary

本次不建議把輸譜的四個功能各自做成互不相關的按鈕。建議先補齊三個共用編輯概念，再由四項功能共用：

1. **插入位置**：沿用現有橘色插入游標，但強化可見性、命名與前／後插入操作。
2. **原子節奏單位**：一般音符是一個單位；四分三連音是由三個可獨立改音高、但結構上不可拆散的單位。
3. **範圍選取與片段剪貼簿**：範圍可以包含一般音符、休止符及完整三連音，並貼到目前插入位置。

版面上維持目前的三欄工作台：

- 中央「我的練唱樂譜」新增一條固定高度的**編輯列**，負責插入位置、範圍選取、複製與貼上。
- 右欄「時值」新增**附點二分（3 拍）**。
- 右欄在時值下方新增「特殊節奏」列，放置**＋四分三連音（3 音＝1 拍）**；它是立即插入三格的動作，不是持續套用的時值選項。
- 練唱頁右下的音格區新增**音格／五線譜**雙視圖；五線譜由同一份 `scoreNotes` 即時生成，完整顯示高音譜號、調號、拍號、時值、休止符、歌詞及三連音。
- 播放全曲與全曲練唱共用一份播放時間線；每次目前音符變更時，同步高亮音格卡、五線譜音符與歌詞，並在需要時捲到下一行。

這樣可以保留現有「先選時值，再按音高輸入」的快速流程，也不會讓使用者誤以為三連音只是單一音符的時值。

## Product Decisions Requiring Approval

以下是本規格為了能直接實作而採用的預設決策；若產品方向不同，應在工程開始前調整：

1. 本期「三連音」只支援**四分音符三連音**：三個音平均播放在一拍內，每格有效長度為 1/3 拍。
2. 新建三連音的三格先沿用插入位置前方最近一個有聲音符的音高；前方沒有有聲音符時使用目前調性的第 1 級。建立後立即進入三格快速設定模式。
3. 三連音的三格可以分別修改音高、八度、升降記號、歌詞及是否為休止符，但不能個別修改時值、搬移或刪除。
4. 範圍複製使用應用程式內部剪貼簿，只在目前頁面工作階段有效；本期不寫入作業系統剪貼簿，也不跨分頁保存。
5. 貼上位置一律採用目前橘色插入游標；沒有另外詢問「要貼在哪裡」的彈窗。
6. 本期不新增永久空白／未完成音格。一般音格仍由使用者在指定插入位置按 1–7 或休止符直接建立，以免播放與匯出出現無音高資料。
7. 「轉樂譜」本期指在練唱頁產生可跟唱的標準五線譜視圖，不反向修改輸譜資料，也不新增另一份樂譜資料來源。
8. 五線譜採用參考專案已驗證的 VexFlow SVG 路線，版本固定後再實作；不使用 Canvas，因 SVG 較適合逐音綁定、高亮、縮放與列印擴充。
9. 練唱頁仍由現有頂部「播放全曲／全曲練唱／停止」控制播放；五線譜視圖不再放一組會互相打架的獨立播放計時器。

## Problem Statement

目前輸譜頁已具備時值選擇、單音插入游標、單音編輯與單步復原，但結構編輯能力不足：

- 時值工具列與音符編輯面板缺少 3 拍的附點二分音符；雖然現有時值顯示邏輯已能辨識數值 3，使用者仍無法從主要介面選擇。
- 樂譜資料目前以獨立音符陣列為主，刪除、游標與選取都只理解單一音符，無法表達「三個音高獨立、節奏結構綁定」的三連音。
- 目前只有單音選取，沒有連續範圍選取與結構化片段剪貼簿，因此無法複製一段旋律到其他位置。
- 中央樂譜其實已在音符之間提供橘色插入標記，按音高也會插入該位置，但標記平時幾乎不可見，缺少位置文字、前／後插入入口與貼上整合，使用者不容易發現。
- 新增三連音後，如果仍讓個別音符任意刪除、個別改時值或把插入游標放到三格中間，資料會產生不完整群組，拍數、播放與後續編輯都可能混亂。
- 練唱頁目前只用簡譜音格卡顯示旋律。雖然全曲播放已透過 `currentPlaybackIndex` 高亮並自動捲動目前卡片，使用者仍無法直接用標準五線譜跟唱。
- 若五線譜另建一套播放計時器，音訊、卡片、調音目標與譜面游標會因瀏覽器排程延遲而逐漸不同步；轉譜結果必須保留回到來源音符的映射，並由同一個播放狀態驅動。
- 現有播放時間函式對每音套用最短 0.18 秒。以本版最高 150 BPM 的四分三連音計算，正確子音時間為 `(1/3) × (60/150) = 0.1333 秒`，舊算法會變成 `max(0.18, 0.1333) = 0.18 秒`，整組成為 `0.18 × 3 = 0.54 秒`，而正確一拍應為 `60/150 = 0.4 秒`。加入三連音時必須把「音樂時間」與「合成器最短包絡」分開。

## Solution

### 1. 中央樂譜新增「編輯列」

在現有樂譜標題列下方新增一條 36–40px、高度固定且捲動時保持可見的編輯列，避免把更多按鈕擠進既有「上一步／下一步／刪除／清空」區域。

編輯列包含：

- 左側狀態：`插入位置：第 2 小節，第 3 音後`、`已選取 8 個音格` 或 `三連音第 1 格設定中`。
- `選取範圍`：切換觸控友善的兩點選取模式。
- `複製`：沒有有效選取時停用。
- `貼上（N 格）`：內部剪貼簿為空時停用；有片段時顯示片段音格數。
- `前方插入`／`後方插入`：選到單音或三連音時顯示，負責把橘色游標移到該原子單位前／後。
- `取消`：在範圍選取或三連音快速設定中顯示。

編輯列只管理結構操作；音高、時值、記號與八度仍留在右欄，維持目前的使用者心智模型。

### 2. 插入標記的視覺與操作

- 每一個可插入邊界都保留一條細線，但命中區至少 24×44px；滑鼠移入或鍵盤聚焦時顯示 `＋`。
- 點擊後顯示持續存在的橘色指揮棒，並同步更新編輯列的位置文字。
- 樂譜開頭、一般音符之間、三連音前後與樂譜結尾都可插入。
- **三連音三格內部不提供插入標記**，避免使用者把其他音符插進群組中間。
- 單擊一般音符後，預設插入位置仍為該音符後方，保持目前行為。
- 單擊三連音任一格後，預設插入位置為整組後方；使用「前方插入」可移到整組前方。
- 設定插入位置後，按右欄音高鍵、鍵盤 1–7／0、貼上片段或新增三連音，都使用同一個插入位置。

### 3. 3 拍音符

- 在右欄「時值」依長到短排列為：全音符、**附點二分（3 拍）**、二分、附點四分、四分、附點八分、八分、十六分。
- 在雙擊音符開啟的編輯面板同步加入 `附點二分（3 拍）`。
- 選取後維持黏著狀態，後續新增的一般音符與休止符皆使用 3 拍，直到使用者切換時值。
- 3 拍音格顯示完整名稱，不只顯示數字 `3`。
- 在 4/4 中，3 拍音符會留下 1 拍；在目前以 3 個四分拍計算的 3/4 與 6/8 中，3 拍音符會填滿一小節。
- 若插入導致小節超拍，沿用目前紅色超拍警示，不阻止輸入。

### 4. 四分三連音建立與編輯

右欄在「時值」下方新增「特殊節奏」列，主要動作標示為：

`＋ 四分三連音　3 音＝1 拍`

操作流程：

1. 使用者先點樂譜中的橘色插入位置。
2. 按下「四分三連音」後，該位置立即出現一個帶括號與數字 `3` 的群組，群組內有三個獨立音格。
3. 三格初始音高沿用插入位置前方最近的有聲音符；若不存在，使用目前調性的第 1 級。三格各自取得下一個歌詞單位；歌詞隊列不足時保持空白。
4. 建立後選中第 1 格並進入「三連音快速設定」；使用 1–7、字母音名、0 或右欄音高鍵時，會**取代目前格的音高**而不是插入新音，完成後自動移到下一格。
5. 第 3 格完成後自動離開快速設定；使用者也可按 Esc、編輯列的「完成」或點擊群組外離開，未覆寫的格保留初始音高。
6. 日後選取群組並按「編輯三連音」，可重新進入三格快速設定；雙擊任一格仍可開啟單格音符編輯面板。

三連音視覺規則：

- 三格外有共同邊框或上方括號，中央顯示 `3`，讓使用者一眼知道是同組。
- 每格仍顯示自己的音名、簡譜數字、歌詞；底部時值統一顯示 `三連音 1/3 拍`。
- 選中單格時突出該格；整組外框仍保持可見。
- 小節拍數只增加 1 拍，不能把三格誤算成 3 拍。
- 顯示寬度以整組可讀為優先，不直接把一般一拍音格寬度硬切成三個無法閱讀的小格；允許小節列水平捲動。

三連音結構規則：

- 三格的音高、八度、臨時記號、歌詞與休止狀態可各自不同。
- 每格有效播放時間固定為 1/3 拍，整組固定為 1 拍。
- 單格音符編輯面板中，時值區改為唯讀說明 `四分三連音子音（固定 1/3 拍）`；不得將其中一格改成一般時值。
- 任一格改為休止符時，該格仍占 1/3 拍，不能顯示或計算為 0 拍。
- 任何刪除入口只要命中其中一格，都刪除整組三格：樂譜標題列的刪除、Backspace、Delete、單格編輯面板與範圍刪除皆同。
- 單格編輯面板的刪除按鈕文字應改為 `刪除整組三連音`，刪除後顯示 `已刪除 1 組三連音（3 格）`。
- 插入游標、貼上及小節排版都把整組當成一個不可拆的原子單位。
- 新增整組、刪除整組與貼上含三連音的片段，各自只建立一筆復原紀錄。

### 5. 範圍選取、複製與貼上

桌面快速流程：

1. 單擊第一格建立選取錨點。
2. Shift＋單擊另一格，選取兩格之間的連續範圍。
3. 按編輯列「複製」或 Ctrl／Cmd＋C。
4. 點擊目的地橘色插入標記。
5. 按「貼上（N 格）」或 Ctrl／Cmd＋V。

觸控／不使用快捷鍵流程：

1. 按「選取範圍」。
2. 點第一格，介面提示 `請選範圍終點`。
3. 點第二格後完成選取並退出兩點選取模式。
4. 按「複製」，再指定插入位置並貼上。

範圍規則：

- 範圍只能是同一份樂譜內的連續音格，允許跨小節。
- 一般音符、休止符、歌詞、時值、升降記號、八度與三連音群組資訊都要複製。
- 選取若碰到三連音任一格，起訖邊界自動擴張到整組，並提示 `已自動包含完整三連音`。
- 複製本身不改變樂譜，也不新增復原紀錄。
- 內部剪貼簿保存複製當下的深層快照；來源之後被修改或刪除，不影響待貼上的片段。
- 貼上永遠插入於橘色游標，不覆蓋目的地既有音符。
- 貼上完成後，貼上的範圍保持選取，游標移到片段後方，方便連續貼上。
- 每次貼上都為片段內的三連音建立新的群組識別碼；不同貼上副本不得因識別碼相同而連動刪除。
- 範圍完成後，現有「刪除」按鈕、Backspace 或 Delete 作用於整個選取範圍；刪除只建立一筆復原紀錄。沒有範圍時才回到刪除游標前一個原子單位的行為。
- 一次貼上不論包含多少音格，只建立一筆復原紀錄。
- 貼上造成超拍時允許完成，並沿用現有小節超拍警示。
- Ctrl／Cmd＋C、Ctrl／Cmd＋V 不得攔截文字輸入框、歌詞欄、曲名欄或其他可編輯文字中的原生複製貼上。
- 重新整理或關閉頁面後，內部剪貼簿清空。

### 6. 指定位置增加音格

本需求不另做「輸入序號」彈窗，而是強化現有直接操作：

1. 使用者點擊欲插入位置的 `＋` 標記，或先選取一個原子單位再按「前方插入／後方插入」。
2. 編輯列顯示明確位置，橘色指揮棒留在該處。
3. 使用者按右欄音高鍵或鍵盤 1–7／0，在該處插入一個使用目前時值、八度與記號設定的音格。
4. 插入後游標移到新音格後方，可連續輸入。

若指定位置相鄰三連音，前／後插入一律以整組為邊界，不得插進三格中間。

### 7. 練唱頁「轉為五線譜」與播放跟隨

#### Placement and view switching

保留目前練唱頁頂部的音色、BPM 與四種練習模式。右下「聽好的聲音」區改名為「練唱樂譜」，在區塊標題列右側加入：

- `▦ 音格`：顯示目前已有的簡譜音格卡。
- `🎼 五線譜`：第一次切換時把目前旋律轉為標準五線譜；之後使用快取結果。
- `跟隨播放`：五線譜模式下顯示，預設開啟。
- `重新排版`：容器尺寸、介面字級或樂譜資料變更而排版未自動更新時使用。

兩個視圖是同一份樂譜的不同呈現，不另存副本。切換視圖不停止播放；若在播放中切到五線譜，生成完成後應立即高亮當下音符。

不建議完全照搬參考專案的獨立大型彈窗，原因是目前練唱頁的調音器、BPM、播放模式與評分提示需要同時可見。第一版採**右下區塊原位切換**，五線譜可在既有區塊內垂直捲動；全螢幕與列印留待後續。

#### Notation content

五線譜至少呈現：

- 曲名與目前 BPM。
- 高音譜號。
- 目前調號；大調／關係小調共用正確的譜首升降記號。
- 4/4、3/4 或 6/8 拍號。
- 一般音符、休止符、附點音符、3 拍附點二分音符與現有可匯入時值。
- 四分三連音的三個音符、共同連音括號與數字 `3`。
- 每個非空白歌詞對齊在對應音符下方；三連音歌詞仍逐格對齊。
- 小節線、終止線與自動換行。

譜面音名拼寫必須依目前調號、調式、唱名級數與臨時記號產生，不能只把 MIDI 值套用固定升降名表；例如降號調與升號調應使用符合調性的拼法。

#### Conversion validation

- 空白樂譜時，五線譜區顯示空狀態 `尚無音符可轉換`，不開空白譜面。
- 只有最後一小節不足拍時允許轉譜，視為未完成尾小節；不得悄悄新增會播放或匯出的假音符。
- 任一小節超拍時，不論是否為尾小節，都停止轉換並列出實際小節與超出拍數，例如 `第 3 小節為 4.5 / 4 拍，請回輸譜頁修正`。原本音格視圖仍可使用。
- 三連音群組資料不完整時沿用匯入驗證，不能在譜面上猜測或只畫兩格。
- 無法映射的時值或音高要顯示明確錯誤與來源音符序號，不得自動改成四分音符或其他近似值。

#### Playback highlight behavior

- `播放全曲` 與 `全曲練唱` 每進入一個新來源音符時，都發出同一個目前音符變更事件。
- 音格視圖沿用現有卡片高亮；五線譜視圖同時高亮對應的符頭、符桿、附點／三連音關聯與歌詞。
- 一般音符使用高對比綠色外框、色彩與光暈；休止符使用琥珀色外框與光暈。不能只靠顏色，需同時有描邊或背景形狀。
- 三連音依序高亮三個子音，不能整組一次亮滿一拍。
- 長音在其完整播放時值內保持高亮，直到下一個來源音符開始。
- 暫停時保留目前高亮，讓使用者知道停在哪裡；繼續播放從同一音符恢復。
- 停止、播放完成、載入另一首樂譜或回到單音模式時清除播放高亮。
- `跟隨播放` 開啟時，只有目前音符接近或離開可視區才捲動，目標位置為視窗上方 30%，可接受範圍為 25–35%；不要每個音符都重新置中造成畫面晃動。
- `跟隨播放` 關閉時仍更新高亮但不自動捲動。
- 系統偏好減少動態效果時使用立即捲動，停用脈衝動畫。

#### Interaction with single-note modes

- 五線譜第一版為唯讀練唱視圖，不在此直接修改樂譜；所有修改回到輸譜頁完成。
- 在 `單音聽音` 或 `單音跟唱` 中點擊譜面音符，應套用現有音格卡的單音選取行為：更新調音目標，並依模式試聽或收音。
- 點擊休止符只更新目前位置，不播放音高。
- 五線譜音符需要鍵盤焦點與可理解的 aria-label，例如 `第 12 音，E♭4，八分音符，歌詞「飛」`。

#### Lessons adopted from the reference project

參考專案 `beatmaker-samba-V2` 的可採用架構如下：

1. 使用 VexFlow 產生 SVG，而非把樂譜畫成不可尋址的點陣圖。
2. 每個渲染後的 SVG 音符保存來源位置資料，建立 `來源位置 → SVG 元素` 的 Map 快取。
3. 播放每次推進時先移除上一個高亮，再從快取取得目前元素加上高亮 class。
4. 高亮元素不在可視區時才自動捲動。
5. 轉譜參數與渲染結果可快取，顯示選項改變時依最後參數重繪。

本專案的調整點：來源鍵由參考專案的「track＋step」簡化為「`scoreNotes` 的來源音符索引」；三連音仍有三個不同來源索引。快取只能加速 DOM 查找，**播放時間線才是唯一時序來源**。

## User Stories

1. As a 輸譜使用者, I want to choose a 3-beat dotted-half duration, so that I can enter melodies that hold for three beats without importing edited JSON.
2. As a 輸譜使用者, I want the 3-beat choice to stay active, so that I can enter several notes of the same duration quickly.
3. As a 輸譜使用者, I want existing notes to be editable to 3 beats, so that I can correct rhythm without deleting and re-entering notes.
4. As a 輸譜使用者, I want to insert one quarter-note triplet with one action, so that I do not have to approximate the rhythm with unsupported decimal durations.
5. As a 輸譜使用者, I want the triplet to appear as three visible cells immediately, so that I can understand and edit its three pitches separately.
6. As a 輸譜使用者, I want keyboard and pitch-pad input to set triplet cells in order, so that entering a triplet remains fast.
7. As a 輸譜使用者, I want to reopen triplet editing later, so that I can correct any one of its pitches.
8. As a 輸譜使用者, I want each triplet cell to have its own lyric, octave and accidental, so that the group can represent the actual melody.
9. As a 輸譜使用者, I want one triplet cell to be allowed to become a rest while keeping its time, so that mixed note/rest triplets are representable.
10. As a 輸譜使用者, I want deleting any triplet cell to delete the whole group, so that no broken two-cell triplets remain.
11. As a 輸譜使用者, I want the delete action to clearly say it will delete the entire triplet, so that the result is not surprising.
12. As a 輸譜使用者, I want undo to restore a deleted triplet in one step, so that structural edits are safely reversible.
13. As a 輸譜使用者, I want a triplet to consume exactly one beat during playback and measure calculation, so that timing remains musically correct.
14. As a 練唱使用者, I want the playhead and scoring flow to visit all three triplet cells, so that I can practice each pitch in sequence.
15. As a 輸譜使用者, I want to select a continuous range with Shift-click, so that desktop editing is fast.
16. As a 觸控使用者, I want a two-point range-selection mode, so that I can select a fragment without a keyboard.
17. As a 輸譜使用者, I want a range that touches a triplet to include the complete group automatically, so that copying never creates corrupted fragments.
18. As a 輸譜使用者, I want copied fragments to retain notes, rests, lyrics and rhythm, so that the pasted phrase matches the source.
19. As a 輸譜使用者, I want to paste the fragment at the visible orange insertion position, so that placement is predictable.
20. As a 輸譜使用者, I want repeated pastes to create independent triplet groups, so that deleting one copy does not affect another.
21. As a 輸譜使用者, I want one paste to be undone in one step, so that large edits are easy to reverse.
22. As a 輸譜使用者, I want to insert before or after a selected note, so that I do not need pixel-perfect clicking between small cards.
23. As a 輸譜使用者, I want insertion targets to show a plus sign on hover or focus, so that I can discover where insertion is possible.
24. As a 鍵盤使用者, I want insertion markers and structural controls to have accessible names and focus states, so that I can edit without a mouse.
25. As a 輸譜使用者, I want overflow to remain visibly warned but not blocked, so that the editor keeps the same permissive workflow as the current version.
26. As a 舊樂譜使用者, I want existing JSON files to continue loading unchanged, so that the feature upgrade does not invalidate my library.
27. As a 樂譜分享者, I want exported JSON to preserve complete triplet structure, so that another copy of the application reproduces the same score.
28. As a 使用者 editing lyrics or titles, I want normal text copy and paste to keep working, so that score shortcuts do not interfere with text fields.
29. As a 練唱使用者, I want to convert the current melody into standard staff notation, so that I can practice by reading a familiar score.
30. As a 練唱使用者, I want the score to show the current clef, key and time signature, so that the notation has the musical context I need.
31. As a 練唱使用者, I want durations, dotted notes, rests and triplets to be rendered faithfully, so that the visual rhythm matches playback.
32. As a 練唱使用者, I want lyrics aligned below their source notes, so that I can sing the correct syllable at the correct time.
33. As a 練唱使用者, I want the currently playing notation note and lyric to be highlighted, so that I can follow the melody without searching the page.
34. As a 練唱使用者, I want triplet children to highlight one by one, so that the visual timing matches the three pitches within one beat.
35. As a 練唱使用者, I want rests to receive a distinct playback highlight, so that I know when to remain silent.
36. As a 練唱使用者, I want the staff to scroll only when the playhead approaches another line, so that the score remains readable rather than constantly moving.
37. As a 練唱使用者, I want to disable follow scrolling while retaining highlights, so that I can inspect another part of the score during playback.
38. As a 練唱使用者, I want pause to leave the current note highlighted, so that I know where playback will resume.
39. As a 練唱使用者, I want to switch between note-cell and staff views without stopping playback, so that I can choose the representation that helps me most.
40. As a 單音練習使用者, I want to click a notation note to select the same practice target as its note cell, so that both views behave consistently.
41. As a 樂譜編輯者, I want conversion errors to identify the exact overfull measure or unsupported source note, so that I can correct the source without guessing.
42. As a 鍵盤或輔具使用者, I want notation notes and playback state to have accessible labels, so that highlighting is not visual-only.

## State and Interaction Model

### Editor states

The editor must have one explicit top-level interaction state at a time:

- `normal`：一般輸入；音高鍵在游標處新增音格。
- `range-select-start`：等待範圍起點。
- `range-select-end`：已選起點，等待終點。
- `range-selected`：範圍完成，可複製或取消。
- `triplet-edit`：三連音單格設定；音高鍵取代目前格並前進。

Opening a text field or note inspector suspends score keyboard shortcuts. Pressing Escape exits range selection or triplet editing before performing any other action.

### Atomic units

- 一般音符與一般休止符各是一個原子單位。
- 完整四分三連音是一個原子單位，內含三個可編輯子音格。
- 插入位置、刪除、小節分組與片段邊界只能落在原子單位之間。
- 播放與練唱仍逐一走訪三個子音格，不把整組當成一個音高。

### Selection precedence

1. 文字輸入焦點最高，不攔截原生快捷鍵。
2. 三連音快速設定次高，音高輸入代表修改目前子格。
3. 範圍選取模式次之，點擊音格代表設定範圍端點。
4. 一般模式下，點擊音格代表選取／試聽，並把插入游標放在其後。

### Practice view and playback states

- 練唱樂譜視圖只有 `cells` 與 `notation` 兩種，切換不改 `activeAppMode` 與播放位置。
- 樂譜渲染狀態分為 `idle`、`rendering`、`ready`、`invalid`；渲染中顯示非阻塞進度，invalid 保留可返回的音格視圖與明確錯誤。
- 播放狀態沿用 `stopped`、`playing`、`paused`，並額外保存 `currentSourceNoteIndex`；所有視圖都讀這個索引，不各自推進。
- `followPlayback` 是純顯示偏好，不影響聲音、評分、播放位置或目前音符高亮。

## Implementation Decisions

### Data model

- 保留現有音符的 `degree`、`octave`、`accidental`、`duration` 與 `lyric` 欄位，避免破壞舊樂譜。
- 三連音的每個子音仍是一般音符資料，但增加可序列化的 `tuplet` 中繼資料，包含群組識別碼 `id`、固定種類 `quarter-triplet`、子格索引 `0–2`、子格總數 `3`、整組占用拍數 `1`。三格共用 `id`，其他欄位按子格一致。
- 三個子音的有效 `duration` 為 1/3 拍；畫面名稱與群組判定必須讀群組中繼資料，不能只靠浮點數 `0.333…` 猜測。
- 樂譜根層新增 `schemaVersion: 2`。沒有 version 的舊檔視為 schema 1 的普通音符資料並保持相容。
- 匯入三連音時驗證：同一群組恰有三筆、彼此相鄰、順序不重複且整組為四分三連音。驗證失敗時不得靜默修補或載入半組；顯示可理解的錯誤，並保持目前樂譜不變。
- 所有載入入口使用相同的正規化與驗證流程，避免內建樂譜、本機檔案與舊匯入入口行為不同。

### Editing operations

- 建立共用的「把索引正規化為原子單位邊界」能力，所有插入、刪除、選取與貼上先經過它。
- 建立共用的「將任意選取範圍擴張到完整原子單位」能力，避免各按鈕各自處理三連音。
- 建立共用的「深層複製片段並重新配置群組識別碼」能力，供貼上與未來可能的重複功能共用。
- 片段快照只複製樂譜語意欄位與群組資料，不複製 `practiceResult`、目前高亮、DOM 狀態或其他練唱階段暫態資料。
- 刪除 API 接收音符索引或範圍後，先解析完整原子單位，再以單一歷史交易修改陣列。
- 三連音子格的音高修改只替換音高相關欄位，不改群組欄位與固定時值。
- 內部剪貼簿不屬於樂譜資料、不參與匯出，也不需要進入 undo／redo。
- Undo／redo 完成後，游標必須移到最近的有效原子邊界，不能留在三連音內部；範圍選取可清除，不要求復原視覺選取狀態。

### Measure layout and timing

- 小節分組以原子單位走訪。四分三連音整組加入同一小節並增加 1 拍，不得因三個 1/3 拍的逐筆計算而被拆到兩個小節。
- 延續現有容許超拍的策略；若目前小節剩餘不足 1 拍，整組留在該小節並顯示超拍，不拆組。
- 拍數加總、比較與顯示集中處理浮點誤差，畫面應顯示 `1`、`3` 等音樂上可理解的數字，不顯示長小數。
- 播放排程逐子格播放 1/3 拍；三格總時間在同一 BPM 下等於普通四分音符一拍。
- 練唱模式的目標音符、高亮、準確音符數與總音符數仍逐子格處理。

### Notation renderer

- 採用 VexFlow SVG renderer。參考專案使用 4.2.0；官方 4.2 快速入門目前示例為固定的 4.2.2，而官方 API 文件另有 5.0.0。預設先以相容性較接近參考專案的 4.2.2 做小型驗證，通過本規格的調號、附點、歌詞、三連音、SVG 元素映射與重繪案例後固定版本；不引用浮動的 latest URL，也不在同一實作混用 4.x／5.x API。
- 建立單向轉換層：`scoreNotes` 與樂譜設定 → notation render model → VexFlow SVG。VexFlow 物件與 SVG DOM 不得回寫成業務資料。
- render model 的每個可播放符號都保存來源音符索引、起始拍、結束拍、小節索引、是否休止與三連音群組資訊。
- VexFlow 畫完後，將來源音符索引寫到該符號的最外層 SVG group，並建立 `sourceNoteIndex → SVG element[]` 的記憶體 Map。若一個來源未來因跨行或連結線產生多個 glyph，Map 仍能容納多個元素。
- 樂譜資料、調號、調式、拍號或容器寬度改變時使渲染快取失效；只改 BPM 不需重畫音符，只更新標題資訊。
- 音高拼寫使用調性與級數的集中轉換器；轉譜、卡片音名與試聽顯示應逐步共用，避免同一音在不同畫面出現矛盾的升降名。
- 一般時值使用標準符值與附點；四分三連音建立 VexFlow tuplet 關係，不將 1/3 拍硬轉成最接近的普通符值。
- 不完整尾小節以非嚴格 voice 或純排版 tick 完成布局；任何排版補位都不可出現在 `scoreNotes`、播放時間線、評分音符數或匯出 JSON 中。

### Shared playback timeline and highlighting

- 在播放開始前由來源資料建立一次扁平 `playbackTimeline`，每筆包含來源音符索引、startBeat、durationBeats、startSeconds、durationSeconds 與目標 MIDI；三連音自然產生三筆連續的 1/3 拍事件。
- 現有音格卡、調音器、音訊、練唱收音視窗與新五線譜都由同一筆 timeline 事件更新。禁止 notation view 自己使用第二個 `setInterval` 或遞增索引。
- 可以延用目前 `currentPlaybackIndex` 對外表示來源音符，但播放推進應以 AudioContext 時間或單調時鐘的絕對時間為準，避免遞迴 timeout 長曲累積漂移。
- `durationBeats × 60 / BPM` 是播放事件邊界的唯一音樂時間公式，不得用最短發聲時間修改。合成器若需要避免 click 或過短 envelope，可縮短 attack／release 或在下一音開始時截斷聲音，但不得延後 timeline 的下一事件。
- 將目前音符變更集中成單一發布點；它負責更新卡片、譜面 Map、高亮 aria-live 文字、調音目標與必要的自動捲動。
- 目前音符變更只更新舊／新元素的 class、aria-current 與必要文字，不得像現行卡片流程一樣在每個音符邊界重建整條音格、中央編輯樂譜或整份 VexFlow SVG。
- 譜面沒有顯示或仍在渲染時，播放不得受阻；渲染完成後依當下 `currentSourceNoteIndex` 補上正確高亮。
- 每次重繪前清空舊 Map 與高亮引用；停止與載入新曲時清空高亮，避免操作已移除的 SVG 節點。
- 自動捲動先以元素與容器的 bounding box 判斷可視性，只有超出上下安全邊界時才執行；不可用固定音符數猜測換行位置。

### UI integration

- 不改動左欄參考圖片與歌詞隊列的主要布局。
- 不把片段操作塞入右欄；右欄維持「輸入參數」，中央編輯列負責「作用位置與結構操作」。
- 不把三連音做成一般黏著時值，避免下一次按音高時不清楚是新增一格還是三格。
- 新增按鈕的 disabled、hover、focus-visible 與 active 狀態需與現有工作台視覺一致。
- 小尺寸版面可讓中央編輯列水平捲動或換成兩列，不得把按鈕縮到難以閱讀。
- 所有圖示按鈕均需有可見文字或 aria-label；狀態變更透過非阻塞訊息及 aria-live 宣告。
- 練唱區的音格／五線譜切換使用同一區塊，不遮住頂部 BPM、模式與停止控制；五線譜內只顯示視圖相關控制，不重複建立播放引擎。
- 五線譜高亮 class 套在音符最外層 SVG group，讓符頭、符桿、附點與歌詞可以一致呈現；休止符使用另一個狀態 class。
- 轉譜錯誤顯示在練唱樂譜區內，提供 `返回音格` 與 `回輸譜頁修正`，不使用只有「確定」的阻塞 alert。

### Import and export compatibility

- 舊版只含普通音符的 JSON 匯入後，其資料內容與播放結果不得改變。
- 新版匯出必須保存三連音群組中繼資料與 schema version。
- 複製／貼上不改曲名、調號、拍號或 BPM，只複製音符片段。
- 不能用相鄰的三個 `duration = 1/3` 自動推定為三連音；只有有效群組資料才具有原子刪除與括號顯示。

## Acceptance Criteria

### 3 拍音符

- [ ] 主要時值列與單音編輯面板都能選到 `附點二分（3 拍）`。
- [ ] 新增音符、休止符及修改既有音符後，資料時值、卡片名稱與寬度都反映 3 拍。
- [ ] 4/4 小節中，3 拍加 1 拍顯示 `4 / 4 拍`；單獨 3 拍顯示 `3 / 4 拍`。
- [ ] 3/4 與目前 6/8 計算模式下，單獨 3 拍顯示滿小節。

### 四分三連音

- [ ] 一次操作在目前游標建立三格，畫面有共同括號與 `3`。
- [ ] 三格可設定不同音高、八度、記號及歌詞。
- [ ] 快速設定時的音高輸入是取代子格，不是插入第四格。
- [ ] 三格總拍數固定為 1 拍，播放總時間等於同 BPM 的四分音符。
- [ ] 在 200 BPM，三連音每格事件邊界為 0.1 秒，整組為 0.3 秒；不受現有 0.18 秒最短值影響。
- [ ] 任一刪除入口命中任何子格時，三格全部刪除，且 undo 一次全部恢復。
- [ ] 任一子格改成休止符仍占 1/3 拍。
- [ ] 三格中間沒有可用插入位置。
- [ ] 三連音接近小節尾端時保持同組，不跨小節拆開。

### 範圍複製與貼上

- [ ] Shift＋單擊與兩點選取模式都能建立跨小節連續範圍。
- [ ] 範圍觸及三連音時自動擴張為完整三格。
- [ ] 複製後修改來源不會改變剪貼簿內容。
- [ ] 貼上保留音高、休止符、歌詞、時值與完整三連音。
- [ ] 貼上不覆蓋既有音符，結果位於橘色游標處。
- [ ] 連續貼上兩次後，兩份三連音具有獨立群組識別碼。
- [ ] 一次 undo 移除整次貼上的所有音格。
- [ ] 選取範圍後執行刪除，整段一次移除並可用一次 undo 恢復。
- [ ] 文字欄位中的 Ctrl／Cmd＋C、Ctrl／Cmd＋V 仍為原生文字操作。

### 指定位置插入

- [ ] 樂譜開頭、結尾及所有原子單位之間都可設定插入位置。
- [ ] 標記滑入／聚焦時顯示 `＋`，選定後持續顯示橘色指揮棒。
- [ ] 編輯列以小節與相對音格文字回報插入位置。
- [ ] 在選定位置輸入一般音符、休止符、三連音及貼上片段，皆落在相同位置。
- [ ] 三連音前後可插入，但群組內不能插入。

### 練唱五線譜與跟隨高亮

- [ ] 練唱樂譜區可在音格與五線譜間切換，切換不改樂譜資料、播放模式與目前播放位置。
- [ ] 五線譜顯示正確的曲名、高音譜號、調號、拍號、小節線、音高、休止符、時值、附點、歌詞與終止線。
- [ ] 3 拍音符顯示為附點二分；四分三連音顯示三個八分形態子音與 `3` 的 tuplet 標記，三格總長一拍。
- [ ] 播放全曲與全曲練唱中，音格卡與五線譜永遠高亮相同來源音符。
- [ ] 三連音依序高亮三格；休止符以不同狀態高亮且不發聲。
- [ ] 暫停保留目前高亮，繼續後接續；停止與播放完成清除高亮。
- [ ] 跟隨播放開啟時只在跨出安全可視區時捲動，關閉時完全不自動捲動但仍高亮。
- [ ] 播放中切到五線譜，渲染完成後高亮當下音符，不從頭重播。
- [ ] 回輸譜頁修改音符、調號或拍號後，回到練唱頁可得到更新後的譜面，不顯示舊快取。
- [ ] 空譜、任一小節超拍、損壞三連音及不支援時值都有具體錯誤，不產生猜測後的譜面。
- [ ] 點擊五線譜音符在兩個單音模式中會選取同一來源音符；每個符號可鍵盤聚焦並有完整 aria-label。
- [ ] 減少動態效果偏好下沒有脈衝動畫，跟隨捲動改為立即定位。

### 回歸

- [ ] 現有單音新增、試聽、雙擊編輯、右鍵歌詞、批次歌詞、刪除、清空、undo／redo 均正常。
- [ ] 內建樂譜與舊版 JSON 載入結果不變。
- [ ] 新版含三連音 JSON 匯出後再匯入，音高、歌詞、群組、拍數與播放順序一致。
- [ ] 練唱頁能逐格播放、高亮並計算三連音子格。
- [ ] 五線譜未開啟、渲染失敗或切回音格時，既有全曲播放與練唱評分仍可正常完成。

## Testing Decisions

### Primary seam

目前專案是單一靜態 HTML 應用，工作區未發現既有自動測試框架。最高且最有價值的測試切面應是**瀏覽器層級的輸譜到練唱工作流**：載入頁面後，以使用者操作點擊／鍵盤輸入，驗證中央音格、拍數、插入位置、匯出 JSON、VexFlow SVG 內容，以及受控播放時間推進時的卡片／譜面高亮。這一個切面可涵蓋 UI、資料、歷史紀錄、匯入匯出、轉譜與播放同步，避免只測內部函式卻漏掉實際事件衝突。

音訊測試使用可控時鐘與假的音訊輸出，驗證排程時間與播放順序，不以人耳或實際揚聲器作為自動化判定。

### Required scenarios

1. 在空白 4/4 樂譜選 3 拍、輸入一音，再輸入 1 拍，驗證滿小節與匯出時值。
2. 在一般音符之間建立三連音，輸入三個不同音高，驗證 3 格、1 拍、播放順序及匯出群組。
3. 從三連音三個不同子格分別觸發各種刪除入口，均只產生一次整組刪除，undo 一次恢復。
4. 把三連音其中一格改成休止符，驗證拍數與播放靜音時間不變。
5. 選取只碰到三連音中間格的範圍，驗證選取自動擴張。
6. 複製包含普通音、休止符、3 拍音與三連音的跨小節範圍，貼到樂譜開頭、中間與結尾。
7. 同一片段連續貼上兩次，刪除其中一份三連音，驗證另一份不受影響。
8. 在歌詞與曲名欄執行文字複製貼上，驗證不觸發樂譜片段功能。
9. 匯入舊版 JSON，驗證音符總數、總拍數與播放順序；再匯出／匯入新版三連音 JSON 做 round trip。
10. 匯入缺一格、順序重複或不連續的三連音資料，驗證顯示錯誤且目前樂譜完全不變。
11. 鍵盤走訪所有插入標記與編輯列按鈕，驗證焦點可見、aria 名稱與狀態宣告。
12. 在窄螢幕驗證編輯列、三連音三格與水平捲動不互相遮蓋。
13. 用 C、G、F、E♭ 大調及至少一個關係小調轉譜，逐音比對來源 MIDI、譜面音名與譜首調號。
14. 將 4、3、2、1.5、1、0.75、0.5、0.25 拍的一般音符與休止符轉譜，驗證符值與附點；不支援值必須報出來源序號。
15. 將含三連音、三連音休止子格與逐格歌詞的樂句轉譜，驗證 tuplet、來源索引與歌詞對齊。
16. 使用假的 AudioContext／單調時鐘推進整首樂曲，在每個邊界驗證卡片、譜面、調音目標三者取得相同來源索引。
17. 播放一個 1 拍音符後接三連音，驗證高亮時間分別為 1 拍、1/3、1/3、1/3 拍，總計 2 拍。
18. 播放中切換音格／五線譜、改變容器寬度觸發重繪、暫停與繼續，驗證時間位置不重設且高亮可恢復。
19. 模擬多行長曲，驗證只有在音符離開安全可視區時自動捲動；關閉跟隨後 scrollTop 不被程式改變。
20. 以空譜、尾小節不足、任一小節超拍及損壞群組測試轉換驗證；任何錯誤不得改寫原始 `scoreNotes`。
21. 在 VexFlow 尚未完成渲染時開始播放，完成後應直接高亮當下音符，且聲音與評分不中斷。

### Test quality rules

- 測試外部可觀察行為，不依賴私有函式名稱、暫時 CSS class 或陣列實作細節。
- 每個數值同時指出來源：畫面拍數、匯出 JSON 或可控時鐘排程；不得只以模糊的視覺寬度判定節奏正確。
- 測試三連音時必須驗證 `3 × 1/3 拍 = 1 拍` 的計算結果及容許的浮點誤差策略。
- 所有結構修改都要驗證 undo／redo 的交易邊界。
- 譜面測試以 SVG 中的來源索引、音符語意與 VexFlow render model 為依據，不用像素截圖猜測音高；另保留少量視覺快照檢查換行、重疊與高亮樣式。
- 播放同步測試使用明確公式：每拍秒數 `60 / BPM`，每音秒數 `durationBeats × 60 / BPM`；測試輸出要能追溯每個邊界的來源音符索引。

## Out of Scope

- 八分音符三連音、二分音符三連音、五連音或其他任意 tuplet。
- 三連音拆組、合組、拖曳重排單一子格或改變整組占用拍數。
- 剪下、拖放複製、非連續多區段選取。
- 跨分頁、跨瀏覽器、跨裝置或作業系統剪貼簿的片段交換。
- 空白未完成音格、缺音高資料的保存與播放。
- 自動修正超拍、自動把超拍三連音移到下一小節或重新量化節奏。
- 和弦、多聲部、連結線、延音線與五線譜雕刻級排版。
- 練唱評分演算法本身的改版；本期只要求三連音能沿用既有逐音評分流程。
- 左欄參考圖片、批次歌詞與練唱頁的整體視覺重設計。
- 在五線譜上直接拖曳、改音高、改歌詞或調整小節；譜面第一版為唯讀投影。
- 五線譜全螢幕彈窗、列印、PDF、MusicXML、MIDI 或圖片匯出。
- 自動加入連結線／延音線、跨小節拆音，以及處理目前資料模型無法明確表達的 pickup measure。
- 以五線譜取代目前音格視圖；兩者在本期並存。

## Further Notes

- 現有 `getDurationLabel` 已包含數值 3 的 `附點二分` 名稱，且內建資料中已有 3 拍音符；本需求的主要缺口是所有可操作入口沒有提供 3 拍選項，以及需確保各入口一致。
- 現有中央樂譜已為音符間建立插入標記，指定位置插入的核心資料操作也已存在。本期應以可發現性與原子邊界為主，不需重做另一套位置輸入系統。
- 現有歷史紀錄只保存音符陣列。實作結構操作時，至少要在 undo／redo 後重新正規化游標與選取，避免索引落入三連音內部。
- 現有音符匯入路徑並非全部使用相同正規化流程；加入群組資料前應統一，否則同一份新版 JSON 可能因載入入口不同而丟失群組中繼資料。
- 若工程排程需要分階段，建議順序為：原子單位與資料驗證 → 3 拍入口 → 指定位置可見性 → 三連音 → 範圍複製貼上 → 五線譜 render model → 播放時間線與雙視圖高亮 → 回歸與無障礙。範圍功能可晚於三連音，但底層必須從一開始就理解原子單位；五線譜則應在三連音資料穩定後再接。
- 參考專案把 VexFlow SVG 節點標成 `track＋step`，用 Map 快取後由播放 tick 加上 `.vf-note-playing`，並只在音符離開可視區時捲動。這是本規格採用的核心參考；本專案只有一條旋律，應改以來源音符索引映射，不保留不必要的多軌複雜度。
- 目前練唱頁已用 `currentPlaybackIndex` 同步音格卡、調音目標與卡片捲動。五線譜高亮應接到這個既有切面，並在後續把其內部時間推進改成絕對時間線；不能只在 VexFlow 模組再複製一份 index 與 timer。
- 參考專案依賴 CDN 載入 VexFlow 4.2.0。實作前需決定是否繼續 CDN 或把固定版依賴隨專案提供；不論採哪種方式，離線／載入失敗時必須保留音格練唱並顯示明確訊息。
- VexFlow 官方資料確認 SVG renderer、Tuplet、Annotation 與取得／標記 SVG element 的 API 路線可行；版本驗證來源見 [VexFlow 官方 GitHub](https://github.com/0xfe/vexflow) 與 [VexFlow API 文件](https://vexflow.github.io/vexflow-docs/index.html)。
